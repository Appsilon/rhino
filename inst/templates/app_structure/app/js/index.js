/*
 * If the following line is highlighted as error,
 * you should install yarn dependencies.
 * To do this, simply run `rhino::lint_js()` once.
 */

/// <reference types="../../.rhino/node/node_modules/@types/rstudio-shiny/" />

/*
 * This is a speciall comment that enables
 * TypeScript checks in JavaScript files.
 * It allows you to maximize the utility of
 * including Shiny type definitions.
 */

// @ts-check
