# How-to: Write JavaScript code

The `app/js/index.js` is the entrypoint. It can import other files. It
must export functions which are to be available in R. Exported objects
need to be referred to with an `App.` prefix in R sources.
